{
  "name": "trmnl-api",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev --persist-to ../.wrangler --port 8000 --inspector-port 9221 --ip 0.0.0.0",
    "start": "wrangler dev --persist-to ../.wrangler --port 8000 --inspector-port 9221 --ip 0.0.0.0"
  },
  "devDependencies": {
    "wrangler": "^4.125.0"
  },
  "dependencies": {}
}
